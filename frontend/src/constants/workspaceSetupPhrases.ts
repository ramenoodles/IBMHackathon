/** Rotating phrases shown while setting up a workspace from a zip archive. */
export const ZIP_SETUP_PHRASES = [
  'Bob is unpacking your archive…',
  'Gnawing through the zip file…',
  'Building a dam from your codebase…',
  'Extracting files into the lodge…',
  'Unzipping with enterprise-grade beaver power…',
  'Bob is stacking logs in the workspace…',
] as const

/** Rotating phrases shown while cloning a GitHub repository. */
export const GITHUB_SETUP_PHRASES = [
  'Bob is cloning from the river of GitHub…',
  'Fetching branches upstream…',
  'Bob is onboarding the remote repo…',
  'Diving into the commit history…',
  'Pulling code into the lodge…',
  'Bob is syncing with the upstream dam…',
] as const

/** Rotating phrases shown while validating a local path. */
export const LOCAL_SETUP_PHRASES = [
  'Bob is validating your local path…',
  'Checking the lodge directory…',
  'Scanning folders on your machine…',
  'Bob is mapping the local territory…',
  'Verifying the codebase is ready to onboard…',
  'Inspecting files in the beaver den…',
] as const

/** Fallback phrases when source type is unknown. */
export const WORKSPACE_SETUP_PHRASES = [
  'Bob is setting up your workspace…',
  'Onboarding your codebase…',
  'Building the lodge for your project…',
  'AI BOB is getting things ready…',
  'Preparing the dam for launch…',
  'Almost ready to explore…',
] as const
